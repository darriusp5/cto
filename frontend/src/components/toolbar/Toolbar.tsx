/**
 * Тулбар редактора (каркас, этап 1).
 * Реализация — этапы 2–4 (раздел 4.3 спецификации).
 */
export function Toolbar(): React.JSX.Element {
  return (
    <div className="flex items-center gap-1 border-b bg-card px-2 py-1.5 text-xs text-muted-foreground">
      Тулбар — этап 2
    </div>
  );
}
