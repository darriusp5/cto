import { Router } from 'express'; import { prisma } from '../lib/prisma'; import { sendSms } from '../lib/sms'; import { signToken } from '../lib/jwt'; import { rateLimit } from '../middleware/rateLimit'; import type { AuthRequest } from '../types';
const router=Router(); const phoneOk=(p:unknown):p is string=>typeof p==='string'&&/^\+375\s?\(?\d{2}\)?[\s-]?\d{3}[\s-]?\d{2}[\s-]?\d{2}$/.test(p); const codeOk=(c:unknown):c is string=>typeof c==='string'&&/^\d{6}$/.test(c);
router.post('/send-code',rateLimit(10,60000),async(req,res)=>{const phone=req.body?.phone;if(!phoneOk(phone)){res.status(400).json({error:'Некорректный телефон'});return;} const last=await prisma.smsCode.findFirst({where:{phone},orderBy:{sentAt:'desc'}});if(last&&Date.now()-last.sentAt.getTime()<60000){res.status(429).json({error:'Повторная отправка через 60 секунд'});return;} const code=process.env.NODE_ENV==='development'?'123456':String(Math.floor(100000+Math.random()*900000));await prisma.smsCode.create({data:{phone,code,expiresAt:new Date(Date.now()+300000)}});await sendSms({phone,code});res.json({message:'Код отправлен'});});
router.post('/verify',async(req,res)=>{const {phone,code}=req.body??{};if(!phoneOk(phone)||!codeOk(code)){res.status(400).json({error:'Некорректные данные'});return;}let rec=await prisma.smsCode.findFirst({where:{phone},orderBy:{sentAt:'desc'}});if(!rec){res.status(400).json({error:'Код не найден'});return;}if(rec.blockedUntil&&rec.blockedUntil.getTime()>Date.now()){res.status(423).json({error:'Вход заблокирован на 5 минут'});return;}if(rec.expiresAt.getTime()<Date.now()){res.status(400).json({error:'Код истёк'});return;}if(rec.code!==code){const attempts=rec.attempts+1;await prisma.smsCode.update({where:{id:rec.id},data:{attempts,blockedUntil:attempts>=5?new Date(Date.now()+300000):null}});res.status(401).json({error:attempts>=5?'Вход заблокирован на 5 минут':'Неверный код'});return;}let user=await prisma.user.findUnique({where:{phone}});user??=await prisma.user.create({data:{phone}});if(user.banned){res.status(403).json({error:'Аккаунт заблокирован',reason:user.banReason});return;}await prisma.user.update({where:{id:user.id},data:{lastActive:new Date()}});res.json({token:signToken({sub:user.id,phone:user.phone,role:user.role==='admin'?'admin':'user'}),user});});
router.post('/refresh',(req,res)=>res.status(501).json({error:'Используйте действующий JWT'})); router.post('/logout',(_req,res)=>res.json({message:'Выход выполнен'})); export default router;
